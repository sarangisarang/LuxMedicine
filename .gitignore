.env
.env.local
*.env
!.env.example

.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

node_modules/
.next/

# Real guideline PDFs, fetched for #41. Multi-megabyte and licensed to their
# publishers — the tests that need them skip when absent.
storage/

# Production secrets — never commit
deploy/.env.prod
# Vertex service-account key (mounted into the api container for EU-region ADC)
deploy/gcp-sa.json

# Int8 ONNX embedding model artifact (~540 MB) — shipped to the server, not committed
models/
